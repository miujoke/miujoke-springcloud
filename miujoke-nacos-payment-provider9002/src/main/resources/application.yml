server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.6:8088 #转到nginx集群


management:
  endpoints:
    web:
      exposure:
        include: '*'